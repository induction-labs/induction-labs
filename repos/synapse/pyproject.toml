[project]
name = "synapse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jonathan Li", email = "30177086+MonliH@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
synapse = "synapse:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
